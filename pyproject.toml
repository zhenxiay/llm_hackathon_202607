[project]
name = "llm-hackathon-202607"
version = "0.1.0"
description = "VIN Parts Finder - decode a VIN and find compatible parts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.31",
    "anthropic[vertex]>=0.40",
]
